export default class Type{
  name
  slug
  icon
  effectiveness

  constructor(name, slug, icon, effectiveness) {
    this.name = name
    this.slug = slug
    this.icon = icon
    this.effectiveness = effectiveness
  }
}
